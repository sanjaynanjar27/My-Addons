/** @odoo-module */

/** Creating cards which we can see containing brief details about any model and its records wes select
 also we are setting dynamic values so we have to compute values to display on cards */
const { Component } = owl;

export class KpiCard extends Component {}

KpiCard.template = "owl.KpiCard";
